"use client";
import { useState } from "react";
import { browserSupabase } from "@dpl/supabase";
import { Logo } from "@dpl/ui";
import "../styles.css";
function siteUrl() { const configured = process.env.NEXT_PUBLIC_SITE_URL || process.env.NEXT_PUBLIC_VERCEL_URL || window.location.origin; return configured.startsWith("http") ? configured.replace(/\/$/, "") : `https://${configured}`; }
export default function LoginPage() { const [email, setEmail] = useState(""); const [message, setMessage] = useState(""); const signIn = async () => { const { error } = await browserSupabase().auth.signInWithOtp({ email, options: { emailRedirectTo: `${siteUrl()}/auth/callback` } }); setMessage(error?.message ?? "Check your email for a magic link."); }; const google = async () => { const { error } = await browserSupabase().auth.signInWithOAuth({ provider: "google", options: { redirectTo: `${siteUrl()}/auth/callback` } }); if (error) setMessage(error.message); }; return <main className="login"><Logo /><div className="login-card"><p className="eyebrow">DPL Fantasy · Botswana</p><h1>Welcome back.</h1><p>Sign in to manage your fantasy club and set your court.</p><button className="google" onClick={google}>Continue with Google</button><div className="or">or use your email</div><input value={email} onChange={(e) => setEmail(e.target.value)} placeholder="you@example.com" type="email" /><button className="primary" onClick={signIn}>Send magic link</button>{message && <small>{message}</small>}</div></main>; }
